# Checked with tools/check-at.py. An entry naming something that moved is ignored rather than
# failing the build, so a stale line reads as working right up until a private access error
# appears somewhere that looks unrelated. Nine of the sixteen entries here were stale after the
# port: three DamageSources.source overloads, a Biome temperature getter and two DripParticle
# constructors that are all public now, a GuiGraphics constructor whose class was renamed, a
# CommandSourceStack constructor Tetra never calls, and AbstractArrow, which moved package.

public-f net.minecraft.world.inventory.Slot x
public-f net.minecraft.world.inventory.Slot y

public net.minecraft.client.particle.DripParticle$FallAndLandParticle
public net.minecraft.client.particle.DripParticle$DripLandParticle

public net.minecraft.server.level.ServerLevel findLightningTargetAround(Lnet/minecraft/core/BlockPos;)Lnet/minecraft/core/BlockPos;

public-f net.minecraft.server.network.ServerGamePacketListenerImpl clientIsFloating
public-f net.minecraft.server.network.ServerGamePacketListenerImpl aboveGroundTickCount

public net.minecraft.world.entity.projectile.arrow.AbstractArrow setPierceLevel(B)V

# Read only, both lost their getters. The arrow's base damage is adjusted for draw strength and
# velocity, and the exhaustion level is sampled each tick to measure what a player spent.
public-f net.minecraft.world.entity.projectile.arrow.AbstractArrow baseDamage
public-f net.minecraft.world.food.FoodData exhaustionLevel

# The custom item model builds a module's layer from a sprite the way a generated item model does.
# UnbakedGeometryHelper.createUnbakedItemElements went with NeoForge's model package, and the
# vanilla method that does the same job is private, as is the only way to hand it texture slots.
public net.minecraft.client.resources.model.cuboid.ItemModelGenerator bake(Lnet/minecraft/client/resources/model/sprite/TextureSlots;Lnet/minecraft/client/resources/model/ModelBaker;Lnet/minecraft/client/renderer/block/dispatch/ModelState;Lnet/minecraft/client/resources/model/ModelDebugName;)Lnet/minecraft/client/resources/model/geometry/QuadCollection;
public net.minecraft.client.resources.model.sprite.TextureSlots <init>(Ljava/util/Map;)V
